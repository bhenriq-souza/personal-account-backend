endpoints:
  createBankAccount:
    summary: Create Bank Account
    requestBody:
      description: "Create a bank account for a given existing bank"
    requestModels:
      application/json: "CreateBankAccountRequest"
    methodResponses:
      - statusCode: 201
        responseModels:
          application/json: CreateBankAccountOkResponse

documentation:
  title: "Personal Account Backend"
  version: "1.2"
  models:
    - name: "CreateBankAccountRequest"
      contentType: application/json
      schema: ${file(schema/bankAccount/CreateBankAccountRequest.json)}
    - name: "CreateBankAccountOkResponse"
      contentType: application/json
      schema: ${file(schema/bankAccount/CreateBankAccountOkResponse.json)}
