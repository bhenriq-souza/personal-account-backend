functions:
  createBankAccount:
    description: ""
    handler: src/bankAccount/bankAccount.create
    events:
      - http:
          path: bank-account
          method: POST
          reqValidatorName: reqValidator
          documentation: ${file(serverless.doc.yml):endpoints.createBankAccount}
